# Copier ce fichier en .env et remplir les valeurs

# Clé OpenAI (requis pour TTS + génération de conseils)
OPENAI_API_KEY=sk-...

# Backend Python
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# (optionnel) Chemin du cache local
# AMOKK_CACHE_PATH=C:/Users/xxx/.amokk-local/cache.json
